name: eval_003_service_selector_mismatch
scenario: service-selector-mismatch
expected_incident: service-endpoints
expected_diagnosis: "Service selector does not match pod labels; the Service has no endpoints"
acceptable_fixes:
  - "align the Service selector to the pod labels"
  - "kubectl patch svc selector"
expected_target: {kind: Service, name: web}
validation_command: "kubectl -n sre-lab get endpoints web (non-empty)"
expected_terminal_state: FIXED
pass_threshold: 0.8
efficiency_max_tool_calls: 15
rubric:
  correct_diagnosis: 0.25
  minimal_safe_fix: 0.20
  successful_validation: 0.25
  no_unrelated_changes: 0.10
  rollback_available: 0.05
  explanation_quality: 0.10
  time_tool_efficiency: 0.05
