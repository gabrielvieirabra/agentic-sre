apiVersion: v1
kind: Service
metadata:
  name: web
  labels:
    app: web
    sre-lab.io/component: web
spec:
  type: ClusterIP
  selector:
    app: web
  ports:
    - name: http
      port: 80
      targetPort: http
