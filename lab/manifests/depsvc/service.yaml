apiVersion: v1
kind: Service
metadata:
  name: depsvc
  labels:
    app: depsvc
    sre-lab.io/component: depsvc
spec:
  type: ClusterIP
  selector:
    app: depsvc
  ports:
    - name: http
      port: 80
      targetPort: http
